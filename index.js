let order = "";
let isConfirmed = false;

while (isConfirmed === false) {
    order = prompt("thé ou café?");
    order = order + " " + prompt("avec ou sans sucre?");
    order = order + " " + prompt("avec ou sans lait?");
    
    console.log(order);


isConfirmed = true;    
}

while (isConfirmed) {

    let drink = "";

    let sugar = "";
    
    let milk = "";

    let milkType = "";

    order = "commande totale"
}
console.log("La Commande est en route");
